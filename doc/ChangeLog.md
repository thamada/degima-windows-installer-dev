# 変更履歴

> **注意**:
>   本ドキュメントは変更履歴です。日付はdateコマンドで確認して2026-01-23 12:34:55のように年-月-日 時:分:秒のようにします。
>   最も最新のものから順に並べて記入します。

## 2026-06-19 02:31:40

- メビウス（縦スクロール STG）を追加（`src/games/mevius.html`）
  - タブ付きシェルに「メビウス」タブを追加（初回選択時に遅延読み込み）
  - 対空（ZAPPER / スペース）・対地（BLASTER / X）の二種武器
  - 空中敵（直進・蛇行・急降下）と地上目標（戦車・対空砲・トーチカ・小型要塞・巨大要塞）
  - 地上背景: 草原・森・道路・ナスカの地上絵（コンドル・クモ・サル・渦巻き・台形）。湖は非表示
  - 地上スクロール方向を上から下へ統一
  - 空中敵と地上目標を色・ラベル・シルエットで視覚的に区別
  - 対空砲・巨大要塞の地上砲弾を銅色の砲弾シルエットに変更し、速度を低速化（1.35 / 1.15）

## 2026-06-19 01:53:47

- ポンゲームに自機ミス時の爆発・溶解演出を追加
  - 左パドルでボールを逃したとき、ボール位置でパーティクル爆発を再生
  - 自機（左パドル）を非表示にし、パドル形状のパーティクルで溶解アニメーションを再生
  - 演出中（約 90 フレーム）はゲーム進行を一時停止し、終了後にボールと自機をリセット
  - CPU 側得点時は従来どおり即座にボールのみリセット

## 2026-06-19 01:44:27

- ビルドコマンドをプラットフォーム別に分割
  - `make build-win` — Windows ポータブル版（`dist/myapp.exe`）
  - `make build-win-nsis` — Windows NSIS インストーラー（`dist/setup.exe`）
  - `make build-mac` — macOS DMG（`dist/myapp.dmg`）
  - `make build` — 上記 3 つをすべて実行
  - `package.json` に `build:win` / `build:win-nsis` / `build:mac` スクリプトを追加
- アプリアイコンを追加
  - `resources/icon.png`（1024×1024、角丸透過）を配置
  - `electron-builder` の `icon` および `main.js` のウィンドウ・Dock アイコンに設定

## 2026-06-19 01:05:10

- インベーダーゲームに自機被弾時の爆発演出を追加
  - 敵弾命中で自機を非表示にし、パーティクルとリングの爆発アニメーションを再生
  - 爆発中はゲーム進行を一時停止し、終了後に残機があれば中央下へ復活
- 画面上部に残機表示（LIVES）を追加
  - 右上に残機数分の小型自機アイコンを表示
  - 被弾・爆発のたびに残機を 1 減らし、アイコン表示も連動して更新

## 2026-06-19 00:59:43

- インベーダーゲームの敵攻撃を強化
  - 射撃確率を引き上げ（`0.018 + wave × 0.005`）、残存敵数に応じてさらに増加
  - 弾速を引き上げ（`5 + wave × 0.6`）
  - 射手を各列の最前列に限定（プレイヤー側の脅威を増加）
  - ウェーブ 2 以降、35% の確率で 2 発同時射撃

## 2026-06-19 00:44:48

- ゲーム画面のスクロール問題を修正
  - シェル・各ゲーム HTML で `overflow: hidden` を設定
  - Canvas を CSS でウィンドウ内に収まるよう自動縮小（`max-height: calc(100% - 1.25rem)`）
  - ゲーム操作キー（矢印キー・スペース等）で `preventDefault()` を呼び、ページスクロールによる操作妨害を防止

## 2026-06-19 00:38:00

- タブ UI をコンパクトなアンダーライン型に刷新
  - タブバー高さ 32px、ネオン glow 等を廃止して視認性と統一感を改善
- ゲーム HTML のレイアウトを調整（縦中央寄せを廃止し、iframe 内で上部が切れないよう変更）

## 2026-06-19 00:30:00

- タブ付きゲームシェル `src/games/index.html` を追加
  - 「インベーダー」「ポン」を画面上部タブで切り替え可能に
  - 各ゲームを iframe で読み込み、ポンは初回選択時に遅延読み込み
- Electron 起動ファイルを `invaders.html` から `index.html` に変更
- ウィンドウタイトルを「ゲーム」に変更

## 2026-06-19 00:25:00

- `electron/` ディレクトリを `src/electron/` に移動
  - `package.json` の `main` を `src/electron/main.js` に更新
  - `electron-builder` の同梱パスを `src/**/*` に統一
  - `main.js` 内のゲーム HTML 参照パスを新構成に合わせて修正
